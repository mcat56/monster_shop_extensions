<h2>Shipping Info:</h2>
<%= form_tag path, method: method do %>
  <%= label_tag :name %>
  <%= text_field_tag :name, current_user.name %>

  <%= label_tag :address %>
  <%= select_tag :address, options_from_collection_for_select(current_user.addresses, 'id', :get_full_address)  %>


  <%= submit_tag button %>
<% end %>
